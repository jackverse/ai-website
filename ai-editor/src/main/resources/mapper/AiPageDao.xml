<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.renren.ai.dao.AiPageDao">
    <select id="listByTenantId" resultType="io.renren.ai.entity.AiPage">
        SELECT * FROM ai_page
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="listByProjectId" resultType="io.renren.ai.entity.AiPage">
        SELECT * FROM ai_page
        WHERE project_id = #{projectId} AND deleted = 0
        ORDER BY created_at DESC
    </select>

    <select id="listByVersionId" resultType="io.renren.ai.entity.AiPage">
        SELECT * FROM ai_page
        WHERE version_id = #{versionId} AND deleted = 0
        ORDER BY created_at DESC
    </select>
</mapper>
